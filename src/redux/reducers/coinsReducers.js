import { GET_COINS, COIN_DETAILS } from "../actions/coinActions";
import { fetchCoins, fetchCoinsDetails } from "../api/coinsApi";

const initialState = { coinsState: [], dataState: [] };

export const coinsReducers = (state = initialState, action) => {
  sw
}